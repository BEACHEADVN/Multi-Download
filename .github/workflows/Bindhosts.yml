name: Check and Upload Bindhosts Update

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *" # 12:00 PM giờ Việt Nam

permissions:
  contents: write

jobs:
  check-and-upload:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ===== PULL STATE =====
      - name: Pull latest
        run: |
          git pull origin main || true

      # ===== GET LATEST RELEASE INFO =====
      - name: Get Latest Release
        id: bindhosts
        run: |
          # Lấy JSON release mới nhất
          JSON=$(curl -s https://api.github.com/repos/bindhosts/bindhosts/releases/latest)
          
          TAG=$(echo "$JSON" | jq -r '.tag_name')
          # Tìm asset có đuôi .zip (thường là file cài đặt của Bindhosts)
          DL_URL=$(echo "$JSON" | jq -r '.assets[] | select(.name | endswith(".zip")) | .browser_download_url' | head -n 1)
          FILE_NAME=$(echo "$JSON" | jq -r '.assets[] | select(.name | endswith(".zip")) | .name' | head -n 1)
          HTML_URL=$(echo "$JSON" | jq -r '.html_url')
          
          if [ -z "$FILE_NAME" ] || [ "$FILE_NAME" = "null" ]; then
             echo "Không tìm thấy file .zip, thử lấy asset đầu tiên..."
             DL_URL=$(echo "$JSON" | jq -r '.assets[0].browser_download_url')
             FILE_NAME=$(echo "$JSON" | jq -r '.assets[0].name')
          fi

          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "dl_url=$DL_URL" >> $GITHUB_OUTPUT
          echo "file_name=$FILE_NAME" >> $GITHUB_OUTPUT
          echo "html_url=$HTML_URL" >> $GITHUB_OUTPUT

      # ===== CHECK STATE =====
      - name: Check State
        id: check
        run: |
          TAG="${{ steps.bindhosts.outputs.tag }}"
          STATE_FILE="bindhosts_state.txt"
          touch $STATE_FILE
          LAST_TAG=$(cat $STATE_FILE)

          if [ "$TAG" = "$LAST_TAG" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      # ===== DOWNLOAD =====
      - name: Download Asset
        if: steps.check.outputs.skip != 'true'
        run: |
          # Tải file với tùy chọn -L để theo dõi chuyển hướng (redirect)
          curl -L -o "${{ steps.bindhosts.outputs.file_name }}" "${{ steps.bindhosts.outputs.dl_url }}"
          
          # Kiểm tra xem file có thực sự tồn tại không
          if [ ! -f "${{ steps.bindhosts.outputs.file_name }}" ]; then
            echo "Lỗi: Không tải được file!"
            exit 1
          fi
          ls -lh "${{ steps.bindhosts.outputs.file_name }}"

      # ===== TELEGRAM UPLOAD =====
      - name: Upload to Telegram
        if: steps.check.outputs.skip != 'true'
        env:
          BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          TAG="${{ steps.bindhosts.outputs.tag }}"
          FILE="${{ steps.bindhosts.outputs.file_name }}"
          URL="${{ steps.bindhosts.outputs.html_url }}"
          
          CAPTION="<b>🚀 Bindhosts New Update!</b>%0A%0AVersion: <code>$TAG</code>%0A%0A<a href=\"$URL\">🔗 Xem chi tiết trên GitHub</a>"

          # Sử dụng đường dẫn tuyệt đối ./ để đảm bảo curl tìm thấy file
          curl -v -F chat_id="${CHAT_ID}" \
               -F document=@"./$FILE" \
               -F caption="$CAPTION" \
               -F parse_mode="HTML" \
               "https://api.telegram.org/bot${BOT_TOKEN}/sendDocument"

      # ===== SAVE & COMMIT =====
      - name: Save new state
        if: steps.check.outputs.skip != 'true'
        run: |
          echo "${{ steps.bindhosts.outputs.tag }}" > bindhosts_state.txt
          git config user.name "actions"
          git config user.email "actions@github.com"
          git add bindhosts_state.txt
          git commit -m "Update bindhosts state to ${{ steps.bindhosts.outputs.tag }}" || exit 0
          git push
